/**
 * Tailwind CSS v4 is CSS-first in this project; app/globals.css is the source of truth.
 * Keep this file as a project map without importing Tailwind configuration types.
 */
const config = {};
export default config;
